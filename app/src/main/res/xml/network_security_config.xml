<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 默认强制 HTTPS，仅允许系统信任的 CA -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
